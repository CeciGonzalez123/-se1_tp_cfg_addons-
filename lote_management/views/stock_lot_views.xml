<odoo>
    <record id="view_stock_lot_tree_inherit" model="ir.ui.view">
        <field name="name">stock.lot.tree.inherit</field>
        <field name="model">stock.lot</field>
        <field name="inherit_id" ref="stock.view_production_lot_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="decoration-danger">is_expired</attribute>
                <attribute name="decoration-success">not expiration_date or not is_expired</attribute>
            </xpath>
            <xpath expr="//tree" position="inside">
                <!-- Incluye el campo expiration_date pero oculto -->
                <field name="expiration_date" invisible="1"/>
                <field name="is_expired" invisible="1"/>
                <button name="action_open_expiration_wizard" 
                        string="Change Expiration Date" 
                        type="object" 
                        class="btn-primary"/>
            </xpath>
        </field>
    </record>
</odoo>







